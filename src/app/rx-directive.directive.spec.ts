import { RxDirectiveDirective } from './rx-directive.directive';

describe('RxDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RxDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
